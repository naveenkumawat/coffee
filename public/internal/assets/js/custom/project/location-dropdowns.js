/**
 * Location dropdowns functionality
 * States are pre-loaded in Form Components, no dynamic loading needed
 */

document.addEventListener("DOMContentLoaded", function () {
    // States are pre-loaded in components, no initialization needed
});
